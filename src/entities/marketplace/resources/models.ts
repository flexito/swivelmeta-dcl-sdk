const modelFolder = "resources/models/";

// -- BUTTONS
export let buyButtonShape =         new GLTFShape(modelFolder + "buy_btn.glb");

// -- STORE
export let storeShape =             new GLTFShape(modelFolder + "store/store.glb");
export let storePortal =            new GLTFShape(modelFolder + "store/store_portal.glb");
export let wearablePlatform =       new GLTFShape(modelFolder + "store/wearable_platform.glb");

// -- WEARABLE MODELS
export let TrekkerGod =          new GLTFShape(`${modelFolder}TrekkerGod.glb`);
export let TrekkerBot =          new GLTFShape(`${modelFolder}TrekkerBot.glb`);
export let MetaTrekker =          new GLTFShape(`${modelFolder}MetaTrekker.glb`);

